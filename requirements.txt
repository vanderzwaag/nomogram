# Analysis pipeline, figures, nomogram rendering and the test suite.
# Pure Python: no system packages, no LaTeX, no compiler.

numpy
scipy
pandas
matplotlib
pytest

# The dashboard is optional and is not needed for any result in the manuscript.
# Install it separately if you want it:
#
#   python -m pip install streamlit
#   streamlit run streamlit_app.py
