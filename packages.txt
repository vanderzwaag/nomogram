# No system packages are required.
#
# The printed nomogram was previously drawn with PyNomo, which pulls in PyX and
# therefore a full LaTeX toolchain plus Ghostscript, and the PDF was assembled
# with ReportLab and PyPDF2. That chain -- build-essential, gfortran,
# libopenblas-dev, liblapack-dev, ghostscript, texlive, texlive-latex-extra,
# dvipng -- existed to render one alignment chart, and it could not run in CI,
# so the printed nomogram was the only output the test suite did not cover.
#
# nomogram_render.py draws the same chart in matplotlib, shares its geometry
# with the interactive version so the two cannot diverge, and is tested.
